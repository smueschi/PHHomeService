"use client";

export default function DashboardPlaceholder() {
    return (
        <div className="p-8 text-center">
            <h1>Dashboard Build Test</h1>
            <p>If you see this, the build infrastructure is fine, and the error is in the DashboardContent logic.</p>
        </div>
    );
}
